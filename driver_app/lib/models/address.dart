class Address
{
  String? formattedAddress;
  String? placeName;
  String? placeId;
  double? latitude;
  double? longitude;

  Address({this.formattedAddress, this.placeName, this.placeId, this.latitude, this.longitude});
}